{
  "name": "media-insight-normalizer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Normalizes asynchronous media-analysis provider results into a stable insight record.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
